import React from 'react'
import { useState, useEffect } from 'react';
import {findLocation} from 


function Locations() {
  return (
    <div></div>
  )
}

export default Locations

// ELEMENTS FOUND ON THIS PAGE
// TEXT: List of Locations 
//Button: Show Locations
//When the "Show Locations" button is clicked, the text should be changed to "Hide Locations".
//When the "Show Locations" button is clicked, all of the locations received from the API should be displayed. Each location should include it's name, climate, and terrain.

//Once Show location is selected 
// Screen 4 buttons : (Button1) Hide Location, (Button2) Sort By Name
// (Button3) Sort By Climate (Button4) Sort by Terrain
// Each Button will display information based on that button