export default function LocationCard({ name, climate, terrain }) {
  <div>
    <ul>
      <li>
        Name: {name}
        Climate: {climate}
        Terrain: {terrain}
      </li>
    </ul>
  </div>;
}
